import { BaseActivityData, Difficulty } from '../../common';

/**
 * {{title}} Etkinliği Veri Yapısı
 */
export interface {{interfaceName}} extends BaseActivityData {
  items: {{itemsName}}[];
  pedagogicalNote: string;
}

export interface {{itemsName}} {
  id: string;
  {{#each itemFields}}
  {{name}}{{#unless required}}?{{/unless}}: {{type}}; // {{description}}
  {{/each}}
}
