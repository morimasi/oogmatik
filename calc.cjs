const fs=require("fs");const content=fs.readFileSync("components/sheets/visual/turkeyMapPaths.ts","utf-8");let regex=/draw:\s*([\x27"])(.*?)\1/g;let match;let minX=Infinity,minY=Infinity,maxX=-Infinity,maxY=-Infinity;while((match=regex.exec(content))!==null){let coords=match[2].match(/[-+]?\d*\.?\d+/g);if(!coords)continue;for(let i=0;i<coords.length;i+=2){if(i+1>=coords.length)break;let x=parseFloat(coords[i]),y=parseFloat(coords[i+1]);if(!isNaN(x)&&!isNaN(y)){if(x>0&&x<2000){minX=Math.min(minX,x);maxX=Math.max(maxX,x);}if(y>0&&y<1500){minY=Math.min(minY,y);maxY=Math.max(maxY,y);}}}}console.log(`Bounds: minX=${minX} minY=${minY} maxX=${maxX} maxY=${maxY}`);
